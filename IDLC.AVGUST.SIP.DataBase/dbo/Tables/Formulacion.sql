CREATE TABLE [dbo].[Formulacion] (
    [idFormulacion] INT           NOT NULL,
    [Descripcion]   VARCHAR (100) NULL,
    CONSTRAINT [PK_Formulacion] PRIMARY KEY CLUSTERED ([idFormulacion] ASC)
);

