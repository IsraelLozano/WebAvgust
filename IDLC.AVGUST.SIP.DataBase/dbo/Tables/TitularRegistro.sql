CREATE TABLE [dbo].[TitularRegistro] (
    [idTitularRegistro]  INT           NOT NULL,
    [nomTitularRegistro] VARCHAR (250) NULL,
    CONSTRAINT [PK_TitularRegistro] PRIMARY KEY CLUSTERED ([idTitularRegistro] ASC)
);

